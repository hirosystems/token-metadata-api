{
  "name": "@stacks/token-metadata-api-client",
  "version": "2.3.1",
  "description": "Client for the Stacks Token Metadata API",
  "author": "Stacks Labs",
  "keywords": [
    "fetch",
    "typescript",
    "swagger-client",
    "@stacks/token-metadata-api-client"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/stx-labs/token-metadata-api.git",
    "directory": "client"
  },
  "license": "GPL-3.0",
  "engines": {
    "node": ">=22"
  },
  "main": "./lib/index.js",
  "typings": "./lib/index.d.ts",
  "unpkg": "lib/index.umd.js",
  "files": [
    "lib"
  ],
  "scripts": {
    "prepublishOnly": "npm run build",
    "build": "rimraf ./lib && npm run build:client --prefix .. && npm run build:node && npm run build:browser",
    "build:node": "tsc && cp -R src/generated lib/generated",
    "build:browser": "microbundle -i src/index.ts -o lib/index.umd.js --no-pkg-main -f umd --external none --globals none --no-compress --tsconfig tsconfig.browser.json --name TokenMetadataApiClient"
  },
  "devDependencies": {
    "@types/node": "25.5.0",
    "microbundle": "0.15.1",
    "rimraf": "6.1.3",
    "typescript": "5.9.3"
  },
  "dependencies": {
    "openapi-fetch": "0.12.2"
  }
}
